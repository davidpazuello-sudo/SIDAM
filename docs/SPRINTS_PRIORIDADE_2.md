# 🟡 SIDAM — Sprints de Prioridade 2

## Status desta branch (24/03/2026)

- [x] P2-A (parcial funcional): hooks para KPIs/lista, cards, gráfico de status e tabela FDA conectados ao `fdaService`.
- [x] P2-B (base): `metaGovService` + hook `useObjectConfig` criados para conexão com `cfg_object_types` e `cfg_object_properties`.
- [x] P2-C (base): Vitest configurado e testes unitários iniciais para `fdaService`, `authService` e `metaGovService`.
- [x] P2-D (repositório): PR template, issue templates, `CONTRIBUTING.md` e `dependabot.yml`.

## Pendências externas (GitHub/Supabase)

- Branch protection do `main` precisa ser aplicada no painel do GitHub.
- Ajustes finos de dados reais dependem da qualidade e volume de dados já inseridos no Supabase.
